<h1>Songs</h1>
<hr>

<% @songs.each do |song| %>
    <p> <%= link_to song.name, artist_songs_path(song.id) %></p>
    <hr>
<% end %>
<%= link_to "Add Song", new_artist_song_path, class: "btn btn-primary" %>
